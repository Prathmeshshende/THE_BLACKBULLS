# Browser calls should stay on /api so Next.js rewrites proxy to backend.
NEXT_PUBLIC_API_BASE_URL=/api

# Set this to your deployed backend URL (for server-side rewrite destination).
BACKEND_BASE_URL=https://your-backend.onrender.com

# Change before production.
DASHBOARD_PASSWORD=admin123
